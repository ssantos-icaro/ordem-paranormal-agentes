services:
  - type: web
    name: ordem-paranormal-agentes
    runtime: node
    plan: free
    autoDeploy: true
    buildCommand: npm install
    startCommand: node server.js
    envVars:
      - key: DATABASE_URL
        value: postgres://POSTGRESQL_USER:PASSWORD@db.XXXX.supabase.co:5432/postgres